---
const links = [
  { href: "/", label: "Home" },
  { href: "/projects", label: "Projects" },
  { href: "/publications", label: "Publications" },
];
---

<nav class="mb-12 flex gap-6 border-b border-slate-200 pb-4">
  {links.map(link => (
    <a
      href={link.href}
      class="text-slate-700 hover:text-slate-900"
    >
      {link.label}
    </a>
  ))}
</nav>